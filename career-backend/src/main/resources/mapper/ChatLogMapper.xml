<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careermate.backend.mapper.ChatLogMapper">

    <insert id="insert">
        INSERT INTO careermate_ai_chat_log (user_id, role, message, topic)
        VALUES (#{userId}, #{role}, #{message}, #{topic})
    </insert>

    <!-- id DESC as a tiebreaker: created_at is second-resolution, so a
         user+assistant pair from the same request round-trip can share the
         exact same timestamp — without this, that pair's relative order
         (which one "came first") is undefined and can come back reversed. -->
    <select id="findRecentForUser" resultType="com.careermate.backend.domain.ChatMessage">
        SELECT * FROM careermate_ai_chat_log
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="topicCountsForUser" resultType="com.careermate.backend.domain.TopicCount">
        SELECT topic, COUNT(*) AS count
        FROM careermate_ai_chat_log
        WHERE user_id = #{userId} AND role = 'user' AND topic IS NOT NULL
        GROUP BY topic
        ORDER BY count DESC
    </select>

    <select id="topicCountsAll" resultType="com.careermate.backend.domain.TopicCount">
        SELECT topic, COUNT(*) AS count
        FROM careermate_ai_chat_log
        WHERE role = 'user' AND topic IS NOT NULL
        GROUP BY topic
        ORDER BY count DESC
    </select>

</mapper>
